# Copy to .dev.vars for local dev (it's gitignored). Leave these out to run without Google sign-in.
# In Google Cloud Console, add http://localhost:5173/api/auth/google/callback as an authorized redirect URI.
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
